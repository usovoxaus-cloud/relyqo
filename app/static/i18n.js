(() => {
 'use strict';
 const valid=value=>value==='uz'||value==='ru';let language='ru';
 const query=new URLSearchParams(location.search).get('lang');
 try{const saved=localStorage.getItem('relyqo_language');language=valid(query)?query:valid(saved)?saved:(navigator.language||'').startsWith('uz')?'uz':'ru';}catch{if(valid(query))language=query;}
 document.documentElement.lang=language;document.cookie='relyqo_language='+language+'; Path=/; SameSite=Lax'+(location.protocol==='https:'?'; Secure':'');
 let dictionary={},pattern=null;
 const normalize=text=>text.replace(/\s+/g,' ').trim();
 const escape=text=>text.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
 function translate(text){if(language!=='uz'||typeof text!=='string'||!/[А-Яа-яЁё]/.test(text))return text;const value=normalize(text);if(dictionary[value])return dictionary[value];return pattern?text.replace(pattern,match=>dictionary[match]||match):text;}
 window.relyqoT=translate;
 const skip='script,style,code,[data-user-content],[translate="no"],#placeName,#placeAddress,#organizationTitle,#organizationLead,#username,#name,#description,#assistantAnswer,#aiText,#analysis,#photoAnalysis';
 function translateNode(node){if(node.nodeType===3){if(!node.parentElement||node.parentElement.closest(skip))return;const t=translate(node.nodeValue);if(t!==node.nodeValue)node.nodeValue=t;}else if(node.nodeType===1&&!node.matches(skip)){for(const attribute of ['placeholder','title','aria-label','alt'])if(node.hasAttribute(attribute)){const v=node.getAttribute(attribute),t=translate(v);if(t!==v)node.setAttribute(attribute,t);}if(!node.matches('textarea,input'))for(const child of [...node.childNodes])translateNode(child);}}
 function ui(){const bar=document.createElement('nav');bar.className='languageBar';bar.setAttribute('aria-label','Til / Язык');const label=document.createElement('label');label.textContent='Til / Язык ';const select=document.createElement('select');select.setAttribute('aria-label','Til / Язык');for(const [value,text]of [['ru','Русский'],['uz','O‘zbekcha']]){const o=document.createElement('option');o.value=value;o.textContent=text;select.append(o);}select.value=language;select.addEventListener('change',async()=>{try{localStorage.setItem('relyqo_language',select.value);}catch{}try{await fetch('/v1/auth/language',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({language:select.value})});}catch{}const url=new URL(location.href);url.searchParams.set('lang',select.value);location.assign(url.pathname+url.search+url.hash);});label.append(select);bar.append(label);document.body.prepend(bar);if(language==='uz'){window.relyqoLanguageReady.then(()=>{translateNode(document.body);const observer=new MutationObserver(records=>{observer.disconnect();for(const r of records){if(r.type==='characterData')translateNode(r.target);else if(r.type==='attributes')translateNode(r.target);else r.addedNodes.forEach(translateNode);}observer.observe(document.body,{subtree:true,childList:true,characterData:true,attributes:true,attributeFilter:['placeholder','title','aria-label','alt']});});observer.observe(document.body,{subtree:true,childList:true,characterData:true,attributes:true,attributeFilter:['placeholder','title','aria-label','alt']});});}}
 window.relyqoLanguageReady=language==='uz'?fetch('/static/i18n-uz.json').then(r=>{if(!r.ok)throw Error();return r.json();}).then(d=>{dictionary=d;const keys=Object.keys(d).filter(k=>/[А-Яа-яЁё]/.test(k)&&!k.includes('<')&&!k.includes('">')).sort((a,b)=>b.length-a.length).map(escape);pattern=new RegExp('(?<![\\p{L}])(?:'+keys.join('|')+')(?![\\p{L}])','gu');document.title=translate(document.title);}).catch(()=>{language='ru';document.documentElement.lang='ru';const selector=document.querySelector('.languageBar select');if(selector)selector.value='ru';return null;}):Promise.resolve();
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ui);else ui();
})();
