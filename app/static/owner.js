const $=s=>document.querySelector(s);$('#issue').onclick=async()=>{try{$('#error').textContent='';$('#result').classList.add('hidden');const reference=$('#ref').value.trim();const response=await fetch('/v1/owner/visit-token',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:$('#password').value,transaction_reference:reference})});const data=await response.json();if(!response.ok)throw new Error(data.detail||'Не удалось создать QR');$('#qr').src=data.qr_url;$('#receipt').textContent=`Чек ${reference}`;$('#result').classList.remove('hidden')}catch(error){$('#error').textContent=error.message}};
